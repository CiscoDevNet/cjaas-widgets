export interface addAliasResponseBody {
    aliases: Array<string>;
    createdAt: string;
    id: string;
    modifiedAt: string;
};

export interface deleteAliasResponseBody {
    aliases: Array<string>;
    createdAt: string;
    id: string;
    modifiedAt: string;
};
